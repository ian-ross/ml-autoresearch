# Run Lifecycle

A local Candidate Experiment submission creates a Harness-owned **Run** directory under the configured runs root.

## Submit command

```bash
ml-autoresearch submit-candidate --candidate path/to/candidate --runs-root runs
```

The command prints JSON containing `run_id`, `run_dir`, `status`, and `rejection_reason`. It exits with status `0` for accepted submissions and non-zero for rejected submissions.

## Run IDs

Run IDs are generated by the Harness, not by the candidate or agent:

```text
run_YYYYMMDD_HHMMSS_<suffix>
```

Example:

```text
run_20260502_153000_ab12cd
```

## Accepted Run layout

```text
runs/
└── run_YYYYMMDD_HHMMSS_<suffix>/
    ├── candidate/
    │   ├── manifest.yaml
    │   └── model.py
    ├── resolved_manifest.yaml
    ├── run_metadata.json
    └── logs/
        └── validation.log
```

The Harness copies accepted candidate source into the Run directory. Later phases execute the copied source, not the original source path.

`resolved_manifest.yaml` is the Harness-owned normalized manifest for the Run.

## Rejected submissions

Validation failures also create a Run directory so humans and agents have stable repair feedback. Rejected Runs contain:

- `run_metadata.json` with `status: rejected` and `rejection_reason`
- `logs/validation.log`

Rejected submissions do not copy the candidate source into `candidate/`.

## Reserved statuses

- `accepted`
- `rejected`
- `smoke_testing`
- `smoke_failed`
- `training`
- `completed`
- `failed`
