request_id: eval_2026_05_31_xwide_dropout_failure_buckets
target_run_id: run_20260530_180658_0af8a8
evaluation_mode: failure_bucket_review
diagnostic_question: >-
  Does the new best extra-wide base-64 U-Net with Line Target auxiliary weight
  0.10 and bottleneck Dropout2d(p=0.10) preserve the plain extra-wide model's
  precision-oriented gains while reducing missed positives and
  false-negative-heavy cases across the whole Working Validation Split, or did
  the aggregate recall improvement trade off into more empty-mask false
  positives or threshold sensitivity?
expected_decision_impact: >-
  Compare the resulting failure buckets and threshold sweep with
  eval_eval_2026_05_30_xwide_unet_failure_buckets for the plain extra-wide
  comparison and eval_eval_2026_05_30_wide_unet_failure_buckets for the base-48
  comparison. If the dropout Run shows fewer or no worse missed-positive and
  false-negative-heavy cases without a material empty-mask false-positive
  penalty, treat run_20260530_180658_0af8a8 as the current in-contract base for
  later refinements. If failure buckets or threshold sweep behavior are worse,
  pause pure architecture changes and pivot toward a human-gated threshold
  selection, scheduler, or early-stopping capability slice.
parameters:
  primary_threshold: 0.5
  batch_size: 8
  artifact_count: 24
  failure_bucket_count: 4
artifact_budget:
  max_artifacts: 24
  max_runtime_seconds: 1800
