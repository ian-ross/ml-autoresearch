request_id: eval_2026_05_30_xwide_unet_failure_buckets
target_run_id: run_20260530_134005_6f20b1
evaluation_mode: failure_bucket_review
diagnostic_question: >-
  Does the current best extra-wide base-64 U-Net with Line Target auxiliary at
  weight 0.10 improve whole-validation failure behavior over the base-48
  comparison target, or is its small aggregate Dice gain mainly a
  precision-oriented tradeoff that increases missed positives, low-recall
  positives, or late-training brittleness outside the saved first-N prediction
  examples?
expected_decision_impact: >-
  Compare the resulting failure buckets with
  eval_eval_2026_05_30_wide_unet_failure_buckets for the base-48 Run. If the
  extra-wide Run shows fewer or no worse missed-positive and false-negative-heavy
  cases, keep base 64 as the current capacity ceiling for subsequent
  in-contract refinements. If failure buckets are marginal or worse, stop width
  scaling and pivot toward training-policy or scheduler/early-stopping capability
  work before further capacity changes.
parameters:
  primary_threshold: 0.5
  batch_size: 8
  artifact_count: 24
  failure_bucket_count: 4
artifact_budget:
  max_artifacts: 24
  max_runtime_seconds: 1800
